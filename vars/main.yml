---
# defaut path to installed Endeca ToolsAndFrameworks (generated by installer)
tf_path: "{{ base_root }}/endeca/ToolsAndFrameworks/{{ tf_version }}"

workbench_user: "admin"
workbench_password_default: "admin"

pid_dir: "/var/run/endeca"

requirements:
  - "unzip"
  - "python-setuptools"
